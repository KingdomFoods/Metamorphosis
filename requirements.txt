# Metamorphosis Phase 2 & 3 — Render-deployable bundle
# (kept independent of the repo-root setup toolkit so app.py ships clean to Render)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
structlog>=24.1.0

# SalesIQ Claude agent (salesiq_agent.py). Router is guarded, so a missing key/SDK won't break the app.
anthropic>=0.69.0
# Optional: shared conversation state across workers/instances (set REDIS_URL). Guarded import.
redis>=5.0.0

# tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
