# ── Secrets — NEVER commit (repo may be public) ──
.env
.env.*
!.env.example          # the placeholder template is safe to commit
*.bak
*.pem
*.key

# Apps Script source holds hardcoded Zoho + Cliq secrets AND doesn't run on Render
# (it's pasted into the Google Sheet editor). Keep it out of the deployed repo entirely.
sheet_sync/

# State / data files that may carry live values
bankfeed_state.json
demo_state.json
*_state.json

# ── PII / customer / financial data — NEVER commit (repo is PUBLIC, confirmed 2026-07-24) ──
# NOTE: patterns must stand ALONE — '#' is a comment only at line start, never inline.
# data/ = tailortalk_*.csv/json (customer names + phones); *.xlsx = lead lists; hdfc_* = bank stmts.
data/
*.xlsx
*.csv
*.json
!package.json
!.vscode/*.json
hdfc_*
kingdom_leads*
sheet_sync/Code.gs

# Python
__pycache__/
*.pyc
.pytest_cache/
