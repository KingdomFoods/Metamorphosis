# ── Secrets — NEVER commit (repo may be public) ──
.env
.env.*
!.env.example          # the placeholder template is safe to commit
*.bak
*.pem
*.key

# Apps Script source holds hardcoded Zoho + Cliq secrets AND doesn't run on Render
# (it's pasted into the Google Sheet editor). Keep it out of the deployed repo entirely.
sheet_sync/

# State / data files that may carry live values
bankfeed_state.json
demo_state.json
*_state.json

# Python
__pycache__/
*.pyc
.pytest_cache/
